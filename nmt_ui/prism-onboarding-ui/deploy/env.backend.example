# Backend environment on the VM (copy to backend/.env)
DATABASE_URL=postgresql://alertuser:alertpass@127.0.0.1/alerts

# Comma-separated browser origins allowed to call the API (include http://YOUR_VM_IP)
CORS_ORIGINS=http://127.0.0.1,http://localhost,http://YOUR_VM_IP,http://10.117.66.44

# Optional: links in emails / callbacks
# API_BASE_URL=http://10.117.66.44
