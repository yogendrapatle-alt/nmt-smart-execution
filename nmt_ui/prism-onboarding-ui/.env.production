# Used only by `vite build` / `npm run build:deploy`.
# Force same-origin API calls (relative `/api/...`) so nginx can proxy on port 80.
# Do NOT set VITE_* to :5000 here — browsers often cannot reach the VM on 5000 (firewall).
VITE_API_BASE_URL=
VITE_BACKEND_URL=
